#block of statements and perform specific task 
#function are use in repeat code redundency is decrese mimes repeat same code possible but attract way

#these is bad programer sine because repeat program is bad but use in fction so these good 
#argument like value supply mines #fuc_sine : "def" and value 10 so not upper footer side 
#pass mines call the value and store it () thats a function in python like:

#function definatio
def cal_sum(a, b): # (a,b) = parameters
    print(a+b)
    return a + b


cal_sum (10,2) #fuction call # (10,2) = arguments mines store the value in parameters

cal_sum(100,100) 

cal_sum(10.5,20.5)

#simple same function use it like fuction def command ok than value noit pass
#value is user return last thing user only input shows in box like (a,b)
#short and user directreturn and than return same print because user showing values in deplay
#than after last line than fucntion name and loit off values enter thats it 

#find average in use fuction

def cal_averga(a,b,c,d):
    print((a+b+c+d)/4)
    return a+b+c+d/4

cal_averga(2,50,40,20)

#14:53 lecture end and next lecture start 15:04

def calsum (a,b):
    print(a+b)
    return (a+b)

cal_sum(10,20)